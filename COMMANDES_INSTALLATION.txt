═══════════════════════════════════════════════════════════════════════
  COMMANDES D'INSTALLATION - À COPIER-COLLER
═══════════════════════════════════════════════════════════════════════

📦 ÉTAPE 1 : INSTALLER LES LOGICIELS
───────────────────────────────────────────────────────────────────────
1. Node.js : Télécharger depuis https://nodejs.org/ (version LTS)
2. PostgreSQL : Télécharger depuis https://www.postgresql.org/download/windows/

Vérifier l'installation :
  node --version
  npm --version
  psql --version

═══════════════════════════════════════════════════════════════════════

🗄️ ÉTAPE 2 : CRÉER LA BASE DE DONNÉES
───────────────────────────────────────────────────────────────────────
psql -U postgres
CREATE DATABASE paintstore;
\q

═══════════════════════════════════════════════════════════════════════

📝 ÉTAPE 3 : CRÉER LE FICHIER .env
───────────────────────────────────────────────────────────────────────
Créer le fichier : backend/.env

Contenu :
───────────────────────────────────────────────────────────────────────
DATABASE_URL="postgresql://postgres:VOTRE_MOT_DE_PASSE@localhost:5432/paintstore"
PORT=5000
NODE_ENV=development
CORS_ORIGIN=http://localhost:3000
JWT_SECRET=your-super-secret-key-change-this-in-production
JWT_EXPIRES_IN=7d
───────────────────────────────────────────────────────────────────────
⚠️ Remplacez VOTRE_MOT_DE_PASSE par votre mot de passe PostgreSQL

═══════════════════════════════════════════════════════════════════════

📦 ÉTAPE 4 : INSTALLER LES DÉPENDANCES
───────────────────────────────────────────────────────────────────────

# Backend
cd backend
npm install

# Frontend
cd ../frontend
npm install

# Racine (optionnel)
cd ..
npm install

═══════════════════════════════════════════════════════════════════════

🔨 ÉTAPE 5 : SETUP PRISMA
───────────────────────────────────────────────────────────────────────
cd backend
npx prisma generate
npx prisma migrate deploy
npx prisma db seed

═══════════════════════════════════════════════════════════════════════

🚀 ÉTAPE 6 : LANCER L'APPLICATION
───────────────────────────────────────────────────────────────────────

# Option 1 : Depuis la racine (les deux en même temps)
cd .. (retour à la racine)
npm start

# Option 2 : Séparément
# Terminal 1 :
cd backend
npm start

# Terminal 2 :
cd frontend
npm start

═══════════════════════════════════════════════════════════════════════

✅ ACCÈS À L'APPLICATION
───────────────────────────────────────────────────────────────────────
Frontend : http://localhost:3000
Backend  : http://localhost:5000

Identifiants par défaut (après seed) :
  Email    : admin@gmail.com
  Password : djalildjt

═══════════════════════════════════════════════════════════════════════

📋 CHECKLIST RAPIDE
───────────────────────────────────────────────────────────────────────
[ ] Node.js installé
[ ] PostgreSQL installé
[ ] Base de données paintstore créée
[ ] Fichier backend/.env créé
[ ] npm install dans backend/
[ ] npm install dans frontend/
[ ] npx prisma generate
[ ] npx prisma migrate deploy
[ ] npx prisma db seed
[ ] Application lancée et accessible

═══════════════════════════════════════════════════════════════════════

🐛 EN CAS DE PROBLÈME
───────────────────────────────────────────────────────────────────────

# Prisma client manquant
cd backend
npx prisma generate

# Réinitialiser la base de données (⚠️ supprime les données)
npx prisma migrate reset
npx prisma migrate deploy
npx prisma db seed

# Vérifier la connexion PostgreSQL
psql -U postgres -d paintstore

═══════════════════════════════════════════════════════════════════════







